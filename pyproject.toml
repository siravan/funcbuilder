# pyproject.toml
[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[project]
name = "funcbuilder"
version = "2.0.1"

[tool.setuptools.packages]
# Pure Python packages/modules
find = { where = ["python"] }


